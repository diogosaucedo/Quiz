import styled from 'styled-components';

const Fieldset = styled.fieldset`
  // box model
  width: 90%;
  padding: 2rem;
  margin-bottom: 1rem;

  // display
  font-family: 'Montserrat', sans-serif;
  font-size: 1.4rem;
  border: 0.1rem solid;
`;

export { Fieldset };
